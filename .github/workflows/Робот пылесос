#Робот пылесос считает и называет количество программистов в комнате 
a=int(input())
if a%10==1:
    if a%100!=11:
        print(a,"программист")
    else:
        print(a,"программистов")
if a%10==2 or a%10==3 or a%10==4:
    if a%100!=12 and a%100!=13 and a%100!=14:
        print(a,"программиста")
    else:
        print(a,"программистов")
if a%10==0 or a%10==5 or a%10==6 or a%10==7 or a%10==8 or a%10==9:
    print(a,"программистов")
